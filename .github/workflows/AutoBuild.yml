name: Auto Build & Deploy

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 3 * * *'  # daily at 3AM UTC

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install Python Dependencies
        run: |
          cd scraper
          pip install -r requirements.txt

      - name: Generate Hugo Content
        run: |
          cd scraper
          python3 generate_hugo_content.py

      - name: Run Markdown Front-Matter Fixer
        run: |
          cd scraper
          python3 fix_existing_tool_pages.py   # <— Make sure this script lives in scraper/
      
      - name: Strictly rebuild front matter
        run: |
          cd scraper
          python3 strict_frontmatter_rebuild.py
          
      - name: Install Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: '0.104.2'

      - name: Build Hugo Site
        run: |
          cd site
          hugo

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          publish_dir: ./site/public
          github_token: ${{ secrets.CYBERFORGE_GH_TOKEN }}
          cname: cyberform.org
